# build output
/geto
/geto-test-*
/build/
*.o
*.a
*.lst

# dub (dub.selections.json is tracked: this is an application, so exact
# dependency versions belong in the repo)
/.dub/

# nix / direnv
/result
/result-*
.direnv/

# editors
.vscode/
.idea/
*.swp
